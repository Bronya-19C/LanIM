# LANIM — 局域网即时通信系统 (User Guide)

## 概述

LANIM（Local Area Network Instant Messenger）是一款运行在**局域网**内的即时通信软件。它不依赖任何中央服务器——同一局域网内的设备通过 **mDNS 自动发现**彼此，通过 **TCP + TLS 加密传输**交换消息和文件。

所有聊天记录在本机通过 **SQLite** 持久化，多个客户端通过**周期性增量同步**达到最终一致的会话视图。

---

## 系统要求

| 项 | 要求 |
|---|------|
| JDK | **25.0.2 LTS** 或更高 |
| 构建工具 | Gradle（项目自带 `gradlew` wrapper，自动下载） |
| 操作系统 | Windows / macOS / Linux |
| 网络 | 同一局域网（同网段，支持 mDNS 多播） |
| 防火墙 | 允许本机进程监听 **TCP 端口** 并发送 **mDNS 多播 (UDP 5353)** |

---

## 快速启动

### 1. 构建项目

```bash
# Windows
gradlew build

# macOS / Linux
./gradlew build
```

首次运行会自动下载 Gradle 和所有依赖，耗时约 1-2 分钟。

### 2. 运行程序

```bash
gradlew run
```

或直接双击执行 `build/libs/LANIM.jar`（需 Java 25 环境）。

期待看到登录窗口：

```
┌──────────────────────────────────────┐
│          LANIM                       │
│  Local Area Network Instant Messenger│
│  ─────────────────────────────────   │
│  Nickname:       [User-A3F2      ]  │
│  Room Secret:    [·············   ]  │
│  ☑ Enable TLS encryption            │
│       [  Connect to Room  ]         │
└──────────────────────────────────────┘
```

---

## 使用指南

### 登录与加入房间

1. **Nickname** — 输入你的显示名称（最长 32 字符）
2. **Room Secret** — 输入房间口令。**所有输入相同口令的用户将进入同一逻辑房间**
3. **TLS Encryption** — 勾选启用 TLS 加密传输（默认启用）
4. 点击 **Connect to Room** 进入主界面

> 房间口令仅在本地用于派生成 `roomId`，不会在网络中以明文传输。但 mDNS 通告中会包含 `roomId` 哈希值（SHA-512），同一局域网的设备可发现房间存在。

### 主界面

```
┌──────────────────────────────────────────────┐
│ LANIM - Alice @ a1b2c3d4...                 │
├────────┬─────────────────────────────────────┤
│Members │  Alice (You)                        │
│(3)     │  Bob [online]                        │
│        │  Charlie [online]                    │
│        │                                     │
│        │  Bob: Hello everyone!               │
│        │  Alice: Hi Bob!                     │
│        │  Charlie sent file: doc.pdf (24 KB) │
│        │                                     │
│        │                                     │
│        ├─────────────────────────────────────┤
│        │ [Type a message...      ] [Send][File]│
└────────┴─────────────────────────────────────┘
```

- **左侧**：房间成员列表，显示在线状态
- **中央**：聊天消息历史
- **底部**：输入框 + 发送按钮 + 文件传输按钮

### 发送消息

在底部输入框键入文本，按 **Enter** 或点击 **Send** 发送。消息会通过 TCP 广播给房间内所有在线成员。

### 发送文件

1. 点击 **File** 按钮
2. 在弹出的文件选择器中选择要发送的文件
3. 文件会自动分片（每片 64KB）并依次传输
4. 接收方会在聊天区域看到文件通告
5. 传输完成后自动校验 SHA-256 完整性

> 文件存储在 `data/files/<fileId>/` 目录下，SQLite 仅保存元数据路径。

### 自动发现与连接

- 程序启动后自动通过 **mDNS** 注册 `_lanim._tcp.local.` 服务
- 自动浏览同类型服务，按 `roomId` 过滤同房间成员
- 发现新成员后主动建立 TCP 连接
- 每 **500ms** 周期性同步未读消息
- 成员离线后自动从列表移除

---

## 网络说明

### mDNS 发现

- 使用标准 mDNS/DNS-SD 协议，端口 **UDP 5353**
- 服务类型：`_lanim._tcp.local.`
- TXT 记录包含 `roomId`（SHA-512 哈希）和 `nickname`
- 仅在同一广播域（子网）内生效

### TLS 加密

- 默认启用 **TLS 1.3** 加密传输
- 每个节点首次启动自动生成 **2048 位 RSA 自签名证书**
- 信任模型：**TOFU**（Trust On First Use）
  - 首次连接某节点时，接受其证书并记录指纹
  - 后续连接验证指纹一致性
  - 指纹变化时会弹出确认对话框

### 传输模式切换

可在登录界面取消勾选 **TLS Encryption** 切换为明文 TCP 模式。**同一房间所有节点必须使用相同的传输模式。**

---

## 配置文件

程序运行过程中生成的本地数据：

| 路径 | 说明 |
|------|------|
| `data/lanim.db` | SQLite 数据库（聊天记录 + 成员信息） |
| `data/files/` | 接收文件的存储目录 |
| `config/keystore.jks` | TLS 自签名证书密钥库 |
| `config/trusted_peers.json` | TOFU 信任的 peer 指纹记录 |

> 这些文件会在首次运行时自动创建。如果删除 `data/lanim.db`，聊天历史将丢失。

---

## 故障排除

### 找不到其他成员

1. 确认所有设备在 **同一子网**（同一路由器 / 交换机下）
2. 检查防火墙是否允许 **mDNS (UDP 5353)** 和 **TCP 动态端口**
3. Windows 用户：尝试在"Windows 防火墙"中允许 Java 进程
4. 多网卡环境：程序默认监听所有网卡，可检查 IP 配置
5. 尝试关闭 TLS 加密（明文模式更易于调试）

### 连接失败

- 确认输入的 **Room Secret** 与其他成员一致
- 检查传输模式（全明或全 TLS）是否一致
- 查看控制台日志输出

### 证书警告

首次连接新节点时，会弹出证书确认对话框。如果看到指纹变化警告，说明目标节点的身份可能改变（或证书已重新生成），需谨慎确认。

---

## 技术概要

| 特性 | 实现方式 |
|------|---------|
| 发现 | mDNS (jmdns) |
| 传输 | TCP + 长度前缀成帧 |
| 加密 | TLS 1.3 (JDK SSLSocket) |
| 序列化 | JSON (Gson) |
| 持久化 | SQLite (xerial-jdbc) |
| 同步 | 水位线增量同步，500ms 周期 |
| 文件 | 64KB 分片 + Base64 + SHA-256 校验 |
| 证书 | 自签名 + TOFU 信任模型 (BouncyCastle) |
| 页面 | JavaFX |

---

*LANIM v1.0 — 局域网即时通信系统*
