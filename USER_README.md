# LANIM — 局域网即时通信系统 (User Guide)

## 概述

LANIM 采用 **客户端—服务端** 架构：

- **服务端**运行在学校提供的远程服务器上（消息中继与历史存储）
- **客户端**由各组员在校园网内的电脑上运行，连接服务端并输入房间口令加入聊天

**课程统一服务端地址：`10.129.245.252:9090`**

---

## 系统要求

| 项 | 要求 |
|---|------|
| JDK | **25** |
| 构建工具 | Gradle（项目自带 `gradlew`） |
| 操作系统 | 客户端：Windows / macOS / Linux；服务端：以远程机环境为准 |
| 网络 | 校园网内可访问 **`10.129.245.252:9090`** |

---

## 组员：启动客户端（常用）

### 1. 构建（首次）

```powershell
cd LanIM
.\gradlew build
```

macOS / Linux：`./gradlew build`

### 2. 运行

```powershell
.\gradlew run
```

### 3. 登录界面

```
┌──────────────────────────────────────────┐
│          LANIM                           │
│  ─────────────────────────────────────   │
│  Server Address: [10.129.245.252:9090]  │
│  Nickname:       [User-A3F2      ]       │
│  Room Secret:    [·············   ]      │
│  ☑ Enable TLS encryption                │
│       [  Connect to Server  ]           │
└──────────────────────────────────────────┘
```

| 字段 | 说明 |
|------|------|
| **Server Address** | 填 **`10.129.245.252:9090`**（不要留空，不要填 localhost） |
| **Nickname** | 显示名称（最长 32 字符） |
| **Room Secret** | 房间口令；**相同口令进入同一房间** |
| **TLS** | 默认勾选；若服务端用 `--plain` 启动则取消勾选 |

### 4. 使用

- **Send / Enter**：发送文字；经服务端转发给同房间其他人
- **File**：选择文件，64 KiB 分片经服务端中继
- 成员上下线自动更新；断线重连后会拉取历史消息

---

## 管理员：远程服务器上启动服务端

> 通常由一位组员或助教在 **`10.129.245.252`** 上操作，其他同学只跑客户端。

```bash
cd LanIM
./gradlew runServer
```

- 默认：**9090** 端口、**TLS** 模式
- 控制台出现 `LANIM Server started on port 9090` 表示就绪

```bash
# 明文模式（联调；所有客户端须取消 TLS）
./gradlew runServer --args="--plain"
```

远程机上的数据目录（勿删除非有意清空历史）：

| 路径 | 说明 |
|------|------|
| `data/lanim.db` | 服务端聊天历史 |
| `config/keystore.jks` | TLS 证书 |

---

## 网络说明

```
你的电脑 (客户端)  ──TCP/TLS──►  10.129.245.252:9090  ──转发──►  其他客户端
```

- 只需能连 **学校服务器**，不要求同学之间互相 ping
- 适合校园网 AP 隔离场景：大家都连中心服务器即可

---

## 故障排除

### 无法连接

1. 确认 **Server Address** 为 `10.129.245.252:9090`
2. 确认已连接 **校园网**（或学校要求的 VPN）
3. 确认远程机上 **runServer 正在运行**
4. PowerShell 自测：
   ```powershell
   Test-NetConnection 10.129.245.252 -Port 9090
   ```
   `TcpTestSucceeded : False` → 网络/VPN/服务端未起，先解决连通性
5. TLS 模式须与服务端一致；可暂用 `--plain` + 取消 TLS 排查

### 看不到其他成员

- 是否连 **同一服务端**、**Room Secret 完全一致**
- 对方是否已成功登录

### TLS / 证书报错

- 远程服务端与本地客户端各自生成 keystore 时，客户端 TOFU 应接受服务端证书
- 联调可改用明文模式排除 TLS

---

## 技术概要

| 特性 | 实现 |
|------|------|
| 拓扑 | Client–Server |
| 服务端 | `10.129.245.252:9090` |
| 传输 | TCP + 长度前缀 JSON |
| 加密 | TLS（默认）/ Plain |
| 持久化 | SQLite |
| 界面 | JavaFX |

---

*LANIM — 课程部署：远程服务端 + 校园网客户端*
